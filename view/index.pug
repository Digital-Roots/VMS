extends layout

block content

.py-5.text-center.cover(style='background-image: url("https://static.pingendo.com/cover-bubble-dark.svg"); background-size: cover;')
  .container
    .row(style='')
      .mx-auto.col-md-6.col-10.bg-white.p-5
        h1.mb-4 Log in
        form
          .form-group
            input#form9.form-control(type='email' placeholder='Enter email')
          .form-group.mb-3
            input#form10.form-control(type='password' placeholder='Password')
            small.form-text.text-muted.text-right
              a(href='#')  Recover password
          button.btn.btn-primary(type='submit') Submit
